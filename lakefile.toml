name = "Strasbourg2026"
version = "0.1.0"
defaultTargets = ["Exercises"]

[[require]]
name = "mathlib"
scope = "leanprover-community"
rev = "v4.31.0-rc1"

[[lean_lib]]
name = "Exercises"

# Solutions.lean is withheld until after the course: the file is kept locally
# but excluded by .gitignore, so its lean_lib target is commented out to keep
# `lake build` working on a fresh clone. Restore both after the school.
# [[lean_lib]]
# name = "Solutions"
